/** Matches backend ALERT_RPM default. */
export const RPM_THRESHOLD = 6000;

/** Matches backend ALERT_TEMP default. */
export const TEMP_THRESHOLD = 90;

/** Number of data points shown in each chart. */
export const WINDOW = 60;

/** Alarm-history polling interval in ms. */
export const POLL_INTERVAL_MS = 10_000;
